Pour déployer ce module dans JBoss : copiez le JAR (reader-services-ejb-0.1-SNAPSHOT.jar) dans le dossier "${JBOSS_HOME}/standalone/deployments/".

Lancez JBoss avec la commande : "${JBOSS_HOME}/bin/standalone.sh" ou "${JBOSS_HOME}/bin/standalone.bat".

Vous pouvez suivre les logs dans le fichier "${JBOSS_HOME}/standalone/log/server.log".
