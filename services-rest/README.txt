Pour déployer ce module dans Tomcat : copiez le WAR (reader-services-rest-0.1-SNAPSHOT.war) dans le dossier "${TOMCAT_HOME}/webapps/"

Lancez Tomcat avec la commande : "${TOMCAT_HOME}/bin/startup.sh" ou "${TOMCAT_HOME}/bin/startup.bat".

Vous pouvez suivre les logs dans le fichier "${TOMCAT_HOME}/logs/catalina.out".
